
#ifndef GAME_STATE_H
#define GAME_STATE_H

// Game state enum used across the project
typedef enum {
    MENU,
    GAMEPLAY,
    CONTROLS,
    PAUSE_MENU,
    QUIT
} GameState;

#endif // GAME_STATE_H

// This was needed due to conflicting Game State errors